{
  "buildCommand": "pip install -r requirements.txt && mkdir -p public && echo '' > public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
